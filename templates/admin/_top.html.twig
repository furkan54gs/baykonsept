<!-- **********************************************************************************************************************************************************
       TOP BAR CONTENT & NOTIFICATIONS
       *********************************************************************************************************************************************************** -->
<!--header start-->
<header class="header black-bg">
    <div class="sidebar-toggle-box">
        <div class="fa fa-bars tooltips" data-placement="right" data-original-title="Toggle Navigation"></div>
    </div>
    <!--logo start-->
    <a href="{{ path ('admin') }}" class="logo"><b>Admin</b></a>
    <!--logo end-->
    <div class="nav notify-row" id="top_menu">
        <!--  notification start -->
        <ul class="nav top-menu">
            <!-- settings start -->
            <li class="toggle">
                <a data-toggle="toggle" class="dropdown-toggle" href="{{ path ('admin_setting_index') }}">
                    <i class="fa fa-cogs">&nbsp;Site Ayarları</i>
                    <span class="badge bg-theme"></span>
                </a>
            <!-- settings end -->
        </ul>
    </div>
    <div class="top-menu">
        <ul class="nav pull-right top-menu">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              <li style="padding-top: 22px;padding-right: 15px">{{ app.user.email }}</li>
                <li><a class="logout" href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
            {% endif %}

        </ul>
    </div>

</header>
<!--header end-->